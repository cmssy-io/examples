# A single standalone app, not a monorepo - but pnpm needs the field to
# accept this file at all on older majors.
packages:
  - .

allowBuilds:
  esbuild: set this to true or false
  sharp: true
  unrs-resolver: true
